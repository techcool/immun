<header class="custom-header-2">
  <div class="custom-header-2__wrapper">

    <!-- LEFT: Logo -->
    <div class="custom-header-2__logo">
      <a href="{{ routes.root_url }}">
        {% if section.settings.logo != blank %}
          <img 
            src="{{ section.settings.logo | image_url: width: 250 }}" 
            alt="{{ shop.name }}">
        {% else %}
          {{ shop.name }}
        {% endif %}
      </a>
    </div>

    <!-- CENTER: Menu -->
    <nav class="custom-header-2__menu">
      {% if section.settings.menu != blank %}
        <ul>
          {% for link in linklists[section.settings.menu].links %}
            <li>
              <a href="{{ link.url }}">{{ link.title }}</a>

              {% if link.links != blank %}
                <ul class="submenu-2">
                  {% for childlink in link.links %}
                    <li>
                      <a href="{{ childlink.url }}">
                        {{ childlink.title }}
                      </a>
                    </li>
                  {% endfor %}
                </ul>
              {% endif %}

            </li>
          {% endfor %}
        </ul>
      {% endif %}
    </nav>

    <!-- RIGHT: Account + Cart -->
    <div class="custom-header-2__icons">
      <a href="{{ routes.account_url }}">Account</a>
      <a href="{{ routes.cart_url }}">
        Cart ({{ cart.item_count }})
      </a>
    </div>

  </div>
</header>

<style>
.custom-header-2 {
  background: #222;
  padding: 18px 40px;
  position: relative;
  z-index: 20;
}

.custom-header-2__wrapper {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

/* Logo */
.custom-header-2__logo img {
  max-height: 50px;
}

/* Menu */
.custom-header-2__menu ul {
  list-style: none;
  display: flex;
  gap: 30px;
  margin: 0;
  padding: 0;
}

.custom-header-2__menu a {
  text-decoration: none;
  color: #fff;
  font-weight: 500;
  font-size: 15px;
  transition: 0.3s;
}

.custom-header-2__menu a:hover {
  color: #0c7c3a;
}

/* Dropdown */
.custom-header-2__menu li {
  position: relative;
}

.submenu-2 {
  position: absolute;
  top: 100%;
  left: 0;
  background: #333;
  padding: 15px;
  display: none;
  flex-direction: column;
  gap: 10px;
  min-width: 180px;
}

.custom-header-2__menu li:hover .submenu-2 {
  display: flex;
}

.submenu-2 a {
  font-size: 14px;
}

/* Icons */
.custom-header-2__icons {
  display: flex;
  gap: 20px;
}

.custom-header-2__icons a {
  color: #fff;
  text-decoration: none;
  font-size: 14px;
}

/* Responsive */
@media (max-width: 991px) {
  .custom-header-2__menu {
    display: none;
  }
}
</style>

{% schema %}
{
  "name": "Custom Menu Header 2",
  "settings": [
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Logo"
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "Select Menu"
    }
  ],
  "presets": [
    {
      "name": "Custom Menu Header 2"
    }
  ]
}
{% endschema %}